<div class="ldoa-background-row {{backgroundColourClassChooser index}}" data-index="{{index}}">
    <div>
        <input class="ldoa-background-checkbox" data-key="{{key}}" type="checkbox">
    </div>
    <div class="ldoa-background-column" data-key="{{key}}">{{name}}</div>
</div>
